class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
       
          // mai khud se comment likh rha chaap nhi rha bsdk 
          //so if whole array is even it is always be gonna true  due to even parity 
          // if there is a single odd number in the array then by keeping the single odd number we can make this whole arrya odd 
          // now if there are more than 2 odd number or 2 odd number we can make awhole array even so in every case it is gonna be true  
        return true ;
    }
};